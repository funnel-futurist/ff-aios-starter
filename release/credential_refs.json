[
  {"name": "ANTHROPIC_API_KEY", "required": false,
   "purpose": "Only needed for API billing or the CI reviewer. A Claude subscription needs no key here."},
  {"name": "OPENAI_API_KEY", "required": false,
   "purpose": "Only if you use embeddings or Whisper transcription."},
  {"name": "GOOGLE_CLIENT_ID", "required": false,
   "purpose": "Only if you connect Google Workspace via API."},
  {"name": "GOOGLE_CLIENT_SECRET", "required": false,
   "purpose": "Only if you connect Google Workspace via API."}
]
