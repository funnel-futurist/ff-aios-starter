# P22 receipt: Workspace Kit and the Febi pilot revision (2026-09-26)

**Written for:** Chat 0 and the P22 lane. It is the evidence behind the P22 return to Phoenix on 2026-09-26.

**Direction.** Founder direction package `FF_FOUNDER_HANDOFF_2026-09-25_V2`, sections 4, 7, 11 and 15, with `DECISIONS_CONFIRMED.md` (2026-09-26). The package was found as loose files in the founder's Downloads folder, not as an extracted folder. Its `SOURCE_MANIFEST.md` was not present, so no source it lists was read, and none is cited here from memory.

## Starting state, refreshed before any change

| Item | State |
|---|---|
| `main` | `8285b5d` (PRs #16 and #17 merged 2026-09-24) |
| 2.6.0 | approved, pinned `0c0cf17` |
| 2.6.1 | DRAFT on PR #18, pinned `8285b5d`, all checks green, **not approved**. Nothing in the V2 package approves it |
| ClickUp | nothing written for the pilot |

## 1. Workspace Kit (PR #19, not merged, not released)

- **Branch and commits:** `exec/p22-workspace-kit-20260926` at `2fac0d1`, based on `8285b5d`.
- **What it is:** `aios workspace plan|init|sync|status`, added to the existing install tool rather than as a second one.
- **Tests:** 21 new tests against real on-disk Git remotes. 141 of 141 pass across the suite.
- **Sanitize:** a release-mode scan at `d4e7606` against the run-time client denylist found 0 findings in 105 files.

**Proved on a real FF canary.** The canary folder is `~/Documents/FF-Operating-System` on the founder's machine. It is not inside any repository, and the shared daily checkout was neither moved nor reset.

| Scenario | Result |
|---|---|
| Init from six manifest entries | Five FF repositories cloned in 66 s, with a partial clone for the 1.5 GB AIOS. Attention was shown as a reference; there is no instance repository yet |
| Dirty checkout | Left intact: HEAD unchanged, no stash, file kept, exit code 5, next action printed |
| Unlocking a phase whose repository does not exist | Reported as "not connected", everything else unaffected, exit 5 |
| Edited map, then regenerated | The edit was kept and the fresh map written as `Repository Map.new.md` |
| Daily checkout | Untouched: same branch, no new worktree |

**Found by the real run, not by the tests.** On the real AIOS repository, Knowledge Health reported 577 missing links. Most were placeholders in code blocks, bare words such as `(URL)`, and links pointing to a home folder or another repository.

- **Fix:** commit `2fac0d1`. Code blocks and inline code are now skipped. A leading slash resolves from the repository root, as it does on GitHub. Links that point outside the repository are counted separately.
- **Result on the same repository:** 206 missing links and 58 pointing outside.

**Not done.** I did not open the vault in the Obsidian app, because that is a person's click. The cloud Architecture Health job and the Command OS preview from direction section 4.12 belong to P21 and P17 and were not built here.

## 2. Febi's project document, revision 2

- **Doc:** `1eKWokaIv8G_nlWtAT4yHrAqJTb8rmI6zNVfD4GrbZAM`, same link, in 01 - Active Projects. It is FF-internal.
- **Checked before overwriting:** the Doc's revision 3 (2026-09-26 06:15) changed only its layout to pageless. Its Markdown export matched the revision-1 source word for word, so nothing was lost.
- **Rendered through Creation's house route:** `ff-creation-system@f8cbf4b`, using `render_document.render(layout='pageless')` and then `writing.Drive.tidy_layout`. It ran from an extracted copy; P04's checkout was not touched.

**Readback, Doc version 20:**
- Content parity with the source is true.
- The Doc is PAGELESS, with 0 empty paragraphs out of 150.
- Heading styles are H1 1, H2 9, H3 20 and H4 10.
- There are 9 native tables.

**Visual check:** the PDF export was inspected page by page.

**What changed in the content:**
- It covers eight tested areas, with a test checklist T1 to T14.
- The environment is clean and client-like, with no FF master.
- No certification is needed before the sandbox.
- The hour figures are marked as estimates.
- It sets out who reviews what, in agreement with P16's recorded rules.
- It records four expected mismatches between the training and the released product (M1 to M4).
- It has an access table and open decisions D1 to D8.

**Unlocked on purpose:** Phoenix needs to comment on it. A later re-render first checks the recorded content hash `46982af2…`, and refuses if anyone has edited the Doc since.

## 3. Findings from checking the certification against the released product

These come from reading the AIOS Deep Operator Cert canon at `ff-delegation@b84365c` against the released Starter.

| # | Finding | Owner |
|---|---|---|
| M1 | The certification's sandbox route is "Use this template". That copies the current main branch, not an approved release, and the copy has no people map, so the founder-only protections do nothing. The copy also carries `releases/`, `tests/` and `evidence/` | P16 (text), P22 (whether template copies should differ) |
| M2 | Branch protection on a free private repository needs a paid GitHub plan. The certification says so; the Starter's founder-only pull-request check is advisory without it | P22 (say so in `/start` and the guide) |
| M3 | Claude Code Web has no `gh`, so the Starter's pre-edit hook cannot identify anyone and holds even a founder to operator limits. The certification recommends Web as the easiest start | P22 (product), P16 (text) |
| M4 | The install tool needs Python 3 and git. Onboarding does not tell a client to install Python | P22 |

## 4. Contracts for other lanes, filed here rather than sent as messages

- **P16:**
  - Certification v1.1 is DRAFT, with no authorized reviewers until adoption. At adoption the reviewers are John and Justine (FD-001).
  - The pilot issues no certificate. It hands P16 a table of mismatches between the training and the product.
  - Ask: name a practice submission route that is not graded, for T11. If none exists, the pilot uses ClickUp only.
- **P04:**
  - The house renderer's footer prints "Funnel Futurist" and the page number with no separator ("Funnel Futurist1"). Pageless Docs hide it; PDF exports show it.
  - Tables use equal column widths, so narrow ID columns waste width.
  - There is no client-mode Creation instance yet. The pilot's Creation side is the client's own asset folder, plus Anthropic's document skills, plus the house renderer as an interface. If P04 supplies a client Creation instance, it joins Febi's workspace as a phase unlock.
- **P01:** the FF canary folder exists. The Workspace Kit's manifest names five FF repositories as instances "not yet split" where the private method and the FF instance share one repository.

## Not done, and why

- **ClickUp:** nothing written. The direction puts the Doc first and ClickUp after it, and the decisions in the Doc's Section 7 are Phoenix's to make.
- **2.6.1:** not approved; no approval was given.
- **Workspace Kit:** not merged and not released. A release containing it (proposed 2.7.0) needs its own founder approval.
- **Febi's GitHub access:** not changed. Decision D7 is recorded in the FF-internal Doc.
